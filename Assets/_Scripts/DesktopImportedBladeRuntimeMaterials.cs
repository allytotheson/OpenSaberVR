using UnityEngine;

/// <summary>
/// Marks an imported blade whose <see cref="Renderer"/> materials were cloned at runtime so we do not duplicate again.
/// </summary>
public sealed class DesktopImportedBladeRuntimeMaterials : MonoBehaviour
{
}
